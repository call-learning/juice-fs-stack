suite: CronJob Disabled
templates:
  - templates/juicefs-backup-cronjob.yaml
tests:
  - it: does not render CronJob when disabled
    set:
      juicefs.backup.enabled: false
    asserts:
      - hasDocuments:
          count: 0
